# Style

- Type hints on all public functions
- Docstrings on public APIs
- Standard Flask layout already in app.py
- Every module carries a module-level `_PROV_CANARY` constant (see
  build.md for the exact shape and binding)
