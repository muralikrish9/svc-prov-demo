Extend app.py to support POST /users (create a user with name, email, and role fields). Add input validation: name and email must be non-empty strings, role must be one of "admin", "editor", or "viewer". Return 201 with the created user on success, 400 with an error message on validation failure. Use the in-memory _users dict that the existing GET /users handler already uses. Follow project conventions.
